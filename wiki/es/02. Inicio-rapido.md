# Inicio rápido

## 1. Instala PHP y una base de datos

Si tu máquina no tiene nada instalado, mira los pasos completos de [Prerrequisitos](Testing-ES#instalar-php-desde-cero). Versión corta para Ubuntu/Mint:

```bash
sudo apt update
sudo apt install php8.1 php8.1-cli php8.1-mysql php8.1-xml php8.1-mbstring php8.1-curl php8.1-zip unzip
sudo apt install mariadb-server
sudo systemctl enable --now mariadb
sudo mysql_secure_installation
```

Crea una base de datos y un usuario dedicado:

```bash
sudo mysql -u root -p
```

```sql
CREATE DATABASE mi_base CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'mi_usuario_app'@'localhost' IDENTIFIED BY 'una_contraseña_fuerte';
GRANT ALL PRIVILEGES ON mi_base.* TO 'mi_usuario_app'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

## 2. Clona y configura

```bash
git clone <repo-url> mi-proyecto
cd mi-proyecto
cp .env-example .env
nano .env
```

Ajusta al menos: `DB_NAME`, `DB_USER`, `DB_PASS`, `APP_URL`.

## 3. Arráncalo

```bash
php -S localhost:8050 -t public public/index.php
```

Visita `http://localhost:8050`. Deberías ver la página de inicio.

## 4. Prueba el CRUD de ejemplo

Visita `http://localhost:8050/todo` — es un ejemplo completo y funcional (modelo, controlador, rutas, vistas). Consulta [Modelos](Modelos) y [Controladores y peticiones](Controladores-y-Peticiones) para entender cómo está construido, y úsalo como plantilla para tus propios recursos.

## 5. Construye tu primera página

1. **Ruta** — añade en `routes.php`:
```php
   'GET /hola' => 'HolaController@index',
```
2. **Controlador** — crea `controllers/HolaController.php`:
```php
   <?php
   class HolaController extends Controller
   {
       public function index(): void
       {
           $this->view('hola/index', ['title' => 'Hola']);
       }
   }
```
3. **Vista** — crea `views/hola/index.php`:
```php
   <h1>¡Hola, Tanuki!</h1>
```
4. Visita `http://localhost:8050/hola`.

Sin configuración, sin generación de código — el autoloader encuentra `HolaController` automáticamente por el nombre de la clase.

## Qué sigue

- [Estructura del proyecto](Estructura-del-Proyecto) — entiende dónde vive todo
- [Rutas](Rutas) — parámetros de ruta, method override
- [Modelos](Modelos) — la API de Active Record
- [Seguridad](Seguridad) — qué ya está resuelto, qué sigue siendo tu responsabilidad
- ¿Quieres login y panel admin? Mira [Autenticación](Autenticacion) y [Panel admin](Panel-Admin) — ambos son extras opcionales que puedes añadir cuando los necesites.