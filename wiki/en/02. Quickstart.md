# Quickstart

## 1. Install PHP and a database

If your machine has nothing installed yet, see the full [Prerequisites](Testing#installing-php-from-scratch) steps. Short version for Ubuntu/Mint:

```bash
sudo apt update
sudo apt install php8.1 php8.1-cli php8.1-mysql php8.1-xml php8.1-mbstring php8.1-curl php8.1-zip unzip
sudo apt install mariadb-server
sudo systemctl enable --now mariadb
sudo mysql_secure_installation
```

Create a database and a dedicated user:

```bash
sudo mysql -u root -p
```

```sql
CREATE DATABASE my_database CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'my_app_user'@'localhost' IDENTIFIED BY 'a_strong_password';
GRANT ALL PRIVILEGES ON my_database.* TO 'my_app_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

## 2. Clone and configure

```bash
git clone <repo-url> my-project
cd my-project
cp .env-example .env
nano .env
```

Set at minimum: `DB_NAME`, `DB_USER`, `DB_PASS`, `APP_URL`.

## 3. Run it

```bash
php -S localhost:8050 -t public public/index.php
```

Visit `http://localhost:8050`. You should see the home page.

## 4. Try the example CRUD

Visit `http://localhost:8050/todo` — this is a complete working example (model, controller, routes, views). See [Models](Models) and [Controllers & Requests](Controllers-and-Requests) to understand how it's built, then use it as a template for your own resources.

## 5. Build your first page

1. **Route** — add to `routes.php`:
```php
   'GET /hello' => 'HelloController@index',
```
2. **Controller** — create `controllers/HelloController.php`:
```php
   <?php
   class HelloController extends Controller
   {
       public function index(): void
       {
           $this->view('hello/index', ['title' => 'Hello']);
       }
   }
```
3. **View** — create `views/hello/index.php`:
```php
   <h1>Hello, Tanuki!</h1>
```
4. Visit `http://localhost:8050/hello`.

No configuration, no code generation — the autoloader finds `HelloController` by its class name automatically.

## What's next

- [Project Structure](Project-Structure) — understand where everything lives
- [Routing](Routing) — route parameters, method override
- [Models](Models) — the Active Record API
- [Security](Security) — what's handled for you, what you still need to do
- Want login and an admin panel? See [Authentication](Authentication) and [Admin Panel](Admin-Panel) — both are optional extras you can add whenever you need them.